#include "main.h"

/**
 * _abs - a function that computes the absolute value
 * @n: integer number
 * Return: absolute value of ab
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
